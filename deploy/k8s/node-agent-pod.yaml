apiVersion: v1
kind: Pod
metadata:
  name: gefi-node-agent
spec:
  containers:
  - name: node-agent
    image: gefi-node-agent:latest
    ports:
    - containerPort: 5000
    env:
    - name: NODE_ID
      value: "node1"